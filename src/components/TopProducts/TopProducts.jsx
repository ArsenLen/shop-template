import React from 'react';

const TopProducts = () => {
    return (
        <div>
            {/* Выводим категории и их картинку /api/categories */}
        </div>
    );
};

export default TopProducts;